---
interface Props {
  href: string;
  label: string;
  light?: boolean;
}

const { href, label, light = false } = Astro.props;
---

<a class:list={["arrow-link", { "arrow-link--light": light }]} href={href}>
  <span>{label}</span>
  <span aria-hidden="true">↗</span>
</a>
